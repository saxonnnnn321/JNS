# Step 2 onwards. Nothing in step 1 needs these — the quote form and PDF work
# with no environment configured at all.

# Supabase (persistence, photo storage, PostGIS parcel geometry)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Step 3: photo assessment
ANTHROPIC_API_KEY=

# Step 2: address lookup + map measurement
NEXT_PUBLIC_MAPBOX_TOKEN=
# NSW Spatial Services needs no key for the public cadastre endpoints.
NSW_CADASTRE_URL=https://maps.six.nsw.gov.au/arcgis/rest/services/public/NSW_Cadastre/MapServer
